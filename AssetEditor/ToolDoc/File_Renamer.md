# File Renamer

Replaces text in file names.

## Example

```text
old text: _diffuse
new text: _base_colour
```

A file named:

```text
unit_body_diffuse.png
```

becomes:

```text
unit_body_base_colour.png
```

Dry run is enabled by default so the resulting names can be reviewed before applying changes.
