# File Cleaner

Deletes matching files from a file or folder selection.

## Options

- Extension, for example `png` or `dds`.
- Include normal maps: files ending in `_n`, `_normal`, or `_normal_map`.
- Include other maps.
- Recursive scan.
- Dry run.

Dry run is enabled by default. Disable it only after reviewing the log.
